#!/usr/bin/env bash
#curl http://localhost:8000/v1/chat/completions -H "Content-Type: application/json" -d "{\"model\": \"default\", \"messages\": [{\"role\": \"user\", \"content\": \"hello\"}], \"temperature\": 0.7}"
#export GATEWAY_SECURITY_API_KEY="111"
#export HF_ENDPOINT="https://hf-mirror.com"
# =============================================================================
# CY-LLM Engine - 统一部署命令行工具
# 用法: ./cy [command] [options] (首选: ./cy 或 ./cy-llm)
# =============================================================================
set -e

# 颜色输出
RED='\033[0;31m'
GREEN='\033[0;32m'
YELLOW='\033[1;33m'
BLUE='\033[0;34m'
CYAN='\033[0;36m'
NC='\033[0m' # No Color

# 项目路径
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
BACKEND_DIR="$SCRIPT_DIR/CY_LLM_Backend"
GATEWAY_DIR="$BACKEND_DIR/gateway"
WORKER_DIR="$BACKEND_DIR/worker"
DEPLOY_DIR="$BACKEND_DIR/deploy"
TRAINING_DIR="$SCRIPT_DIR/CY_LLM_Training"

# 默认配置
DEFAULT_ENV_NAME="${CY_LLM_CONDA_ENV:-vllm}"
DEFAULT_PYTHON_VERSION="${CY_LLM_PYTHON_VERSION:-3.10}"
DEFAULT_PORT="${CY_LLM_PORT:-8080}"
DEFAULT_WORKER_PORT="${CY_LLM_WORKER_PORT:-50051}"
DEFAULT_LITE_PORT="${CY_LLM_LITE_PORT:-8000}"
DEFAULT_LITE_COORDINATOR_PORT="${CY_LLM_LITE_COORDINATOR_PORT:-50051}"
DEFAULT_LITE_WORKER_PORT="${CY_LLM_LITE_WORKER_PORT:-50052}"
DEFAULT_ENGINE="${CY_LLM_ENGINE:-cuda-vllm}"
DEFAULT_MODEL="${CY_LLM_MODEL:-facebook/opt-2.7b}"
DEFAULT_CHARACTER="${CY_LLM_CHARACTER:-芙宁娜}"
DEFAULT_BATCH_SIZE="${CY_LLM_BATCH_SIZE:-2}"
DEFAULT_GRAD_ACCUM="${CY_LLM_GRAD_ACCUM:-8}"
DEFAULT_EPOCHS="${CY_LLM_EPOCHS:-3}"

PIP_MIRROR="https://pypi.tuna.tsinghua.edu.cn/simple"
PIP_ARGS="--index-url $PIP_MIRROR --trusted-host pypi.tuna.tsinghua.edu.cn"

# 引擎到依赖文件的映射
declare -A ENGINE_REQUIREMENTS=(
    ["cuda-vllm"]="requirements-vllm.txt"
    ["cuda-vllm-async"]="requirements-vllm.txt"
    ["cuda-trt"]="requirements-trt.txt"
    ["nvidia"]="requirements-nvidia.txt"
    ["cuda"]="requirements-nvidia.txt"
    ["ascend-vllm"]="requirements_ascend.txt"
    ["ascend-mindie"]="requirements_ascend.txt"
)

# =============================================================================
# 辅助函数
# =============================================================================

info()    { echo -e "${BLUE}[INFO]${NC} $*"; }
success() { echo -e "${GREEN}[OK]${NC} $*"; }
warn()    { echo -e "${YELLOW}[WARN]${NC} $*"; }
error()   { echo -e "${RED}[ERROR]${NC} $*" >&2; }
step()    { echo -e "\n${CYAN}━━━ $* ━━━${NC}"; }

check_command() {
    command -v "$1" &>/dev/null
}

get_conda() {
    if check_command conda; then
        echo "conda"
    elif check_command mamba; then
        echo "mamba"
    elif [[ -f "$HOME/miniforge3/bin/conda" ]]; then
        echo "$HOME/miniforge3/bin/conda"
    elif [[ -f "$HOME/miniconda3/bin/conda" ]]; then
        echo "$HOME/miniconda3/bin/conda"
    else
        error "Conda/Mamba 未找到，请先安装 Miniforge/Miniconda"
        exit 1
    fi
}

get_python_path() {
    local env_name="${1:-$DEFAULT_ENV_NAME}"
    local conda_cmd=$(get_conda)
    $conda_cmd run -n "$env_name" which python 2>/dev/null
}

# =============================================================================
# 命令: setup - 初始化环境
# =============================================================================
cmd_setup() {
    local env_name="$DEFAULT_ENV_NAME"
    local python_version="$DEFAULT_PYTHON_VERSION"
    local engine="$DEFAULT_ENGINE"
    local skip_gradle=false
    local skip_deps=false

    while [[ $# -gt 0 ]]; do
        case $1 in
            --env)      env_name="$2"; shift 2 ;;
            --python)   python_version="$2"; shift 2 ;;
            --engine)   engine="$2"; shift 2 ;;
            --skip-gradle) skip_gradle=true; shift ;;
            --skip-deps)   skip_deps=true; shift ;;
            *) shift ;;
        esac
    done

    # 验证引擎类型
    if [[ ! -v ENGINE_REQUIREMENTS["$engine"] ]]; then
        error "不支持的引擎类型: $engine"
        echo "支持的引擎: ${!ENGINE_REQUIREMENTS[@]}"
        exit 1
    fi

    local req_file="${ENGINE_REQUIREMENTS[$engine]}"

    step "🚀 CY-LLM Engine 环境初始化"
    info "引擎: $engine"
    info "环境名称: $env_name"
    info "依赖文件: $req_file"
    local conda_cmd=$(get_conda)
    info "使用包管理器: $conda_cmd"

    # 1. 创建/检查 Conda 环境
    step "1/3 Python 环境"
    if $conda_cmd env list | grep -q "^$env_name "; then
        success "环境 '$env_name' 已存在"
    else
        info "创建环境 '$env_name' (Python $python_version)..."
        $conda_cmd create -n "$env_name" python="$python_version" -y
        success "环境创建完成"
    fi

    # 2. 安装 Python 依赖
    if [[ "$skip_deps" != true ]]; then
        step "2/3 安装依赖: $req_file"
        if [[ -f "$SCRIPT_DIR/$req_file" ]]; then
            info "安装 Python 依赖..."

            # 按行安装 requirements，以便输出每个包的安装状态。
            # 空行、注释行和以 - 开头的 pip 选项将被跳过（保留给整体安装回退）。
            install_failed=false
            while IFS= read -r req_line || [[ -n "$req_line" ]]; do
                # 去掉首尾空白
                line_trimmed=$(echo "$req_line" | sed -e 's/^[[:space:]]*//' -e 's/[[:space:]]*$//')
                # 跳过空行和注释
                if [[ -z "$line_trimmed" || "$line_trimmed" =~ ^# ]]; then
                    continue
                fi
                # 如果是 pip 选项（例如 --extra-index-url 或 -f），跳过逐行安装
                if [[ "$line_trimmed" =~ ^- ]]; then
                    continue
                fi

                # 显示安装开始信息（尽量提取包名==版本或显示整行）
                echo -e "${YELLOW}正在安装 ${line_trimmed}${NC}"

                if ! $conda_cmd run -n "$env_name" pip install --no-cache-dir \
                    $PIP_ARGS \
                    "$line_trimmed" -q; then
                    warn "安装 ${line_trimmed} 失败，回退使用整体 requirements 安装以获得完整错误信息"
                    install_failed=true
                    break
                fi

                echo -e "${GREEN}安装完成: ${line_trimmed}${NC}"
            done < "$SCRIPT_DIR/$req_file"

            if $install_failed; then
                # 回退到一次性安装以显示 pip 的完整错误输出
                $conda_cmd run -n "$env_name" pip install \
                $PIP_ARGS \
                -r "$SCRIPT_DIR/$req_file"
            fi

            success "依赖安装完成"
        else
            warn "依赖文件不存在: $req_file"
        fi

        # 为 TRT 添加特殊提示
        if [[ "$engine" == "cuda-trt" ]]; then
            echo ""
            echo "${YELLOW}⚠️  TensorRT-LLM 需要手动安装${NC}"
            echo "  方法 1: pip install tensorrt_llm -U --pre --extra-index-url https://pypi.nvidia.com"
            echo "  方法 2: 使用 NGC 容器 nvcr.io/nvidia/tensorrt-llm:latest"
            echo "  文档: https://github.com/NVIDIA/TensorRT-LLM"
            echo ""
        fi
    else
        info "跳过依赖安装"
    fi
    success "依赖安装完成"
    
    # 3. 构建 Gateway (可选)
    if [[ "$skip_gradle" == false ]]; then
        step "2/3 Gateway 构建"
        if check_command java; then
            info "构建 Gateway JAR..."
            (cd "$GATEWAY_DIR" && ./gradlew build -x test --quiet)
            success "Gateway 构建完成"
        else
            warn "Java 未安装，跳过 Gateway 构建"
        fi
    else
        info "跳过 Gateway 构建"
    fi
    
    # 4. 完成
    step "3/3 验证"
    local python_path=$(get_python_path "$env_name")
    success "Python: $python_path"
    
    echo ""
    success "✨ 环境初始化完成！"
    echo ""
    echo -e "下一步: ${CYAN}./cy-llm start${NC} 启动服务"
    echo -e "        ${CYAN}./cy-llm worker${NC} 仅启动 Worker"
}

# =============================================================================
# 命令: start - 启动完整服务 (Gateway + Worker)
# =============================================================================
cmd_start() {
    local env_name="$DEFAULT_ENV_NAME"
    local port="$DEFAULT_PORT"
    local worker_port="$DEFAULT_WORKER_PORT"
    local engine="$DEFAULT_ENGINE"
    local daemon=false
    
    while [[ $# -gt 0 ]]; do
        case $1 in
            --env)      env_name="$2"; shift 2 ;;
            --port)     port="$2"; shift 2 ;;
            --worker-port) worker_port="$2"; shift 2 ;;
            --engine)   engine="$2"; shift 2 ;;
            -d|--daemon) daemon=true; shift ;;
            *) shift ;;
        esac
    done

    step "🌟 启动 CY-LLM Engine 服务"
    
    local python_path=$(get_python_path "$env_name")
    if [[ -z "$python_path" ]]; then
        error "环境 '$env_name' 未找到，请先运行: ./cy setup"
        exit 1
    fi
    
    local jar_path="$GATEWAY_DIR/build/libs/gateway-0.0.1-SNAPSHOT.jar"
    if [[ ! -f "$jar_path" ]]; then
        error "Gateway JAR 未找到，请先运行: ./cy setup"
        exit 1
    fi
    
    info "Engine: $engine"
    info "Gateway Port: $port"
    info "Worker Port: $worker_port"
    info "Python: $python_path"

    # 统一环境变量（仅使用 CY_LLM_*）
    export CY_LLM_ENGINE="$engine"

    # Ensure VLLM GPU memory env vars are set for local starts (default to 0.8)
    export VLLM_GPU_MEM=${VLLM_GPU_MEM:-0.8}
    export VLLM_GPU_MEMORY_UTILIZATION=${VLLM_GPU_MEMORY_UTILIZATION:-$VLLM_GPU_MEM}

    # CY_LLM model registry path
    export CY_LLM_MODEL_REGISTRY_PATH="${CY_LLM_MODEL_REGISTRY_PATH:-\"$SCRIPT_DIR/CY_LLM_Backend/deploy/models.json\"}"
    
    if [[ "$daemon" == true ]]; then
        info "后台模式启动..."
        java -jar "$jar_path" \
            --server.port="$port" \
            --self-deploy.enabled=true \
            --self-deploy.python-executable="$python_path" \
            --self-deploy.worker-dir="$WORKER_DIR" \
            --self-deploy.worker-port="$worker_port" \
            > /tmp/cy-llm-gateway.log 2>&1 &
        echo $! > /tmp/cy-llm-gateway.pid
        success "Gateway PID: $(cat /tmp/cy-llm-gateway.pid)"
        success "日志: /tmp/cy-llm-gateway.log"
    else
        java -jar "$jar_path" \
            --server.port="$port" \
            --self-deploy.enabled=true \
            --self-deploy.python-executable="$python_path" \
            --self-deploy.worker-dir="$WORKER_DIR" \
            --self-deploy.worker-port="$worker_port"
    fi
}

# =============================================================================
# 命令: worker - 仅启动 Worker
# =============================================================================
cmd_worker() {
    local env_name="$DEFAULT_ENV_NAME"
    local port="$DEFAULT_WORKER_PORT"
    local engine="$DEFAULT_ENGINE"
    local model=""
    local serve=true
    
    while [[ $# -gt 0 ]]; do
        case $1 in
            --env)      env_name="$2"; shift 2 ;;
            --port)     port="$2"; shift 2 ;;
            --engine)   engine="$2"; shift 2 ;;
            --model)    model="$2"; shift 2 ;;
            --no-serve) serve=false; shift ;;
            *) shift ;;
        esac
    done

    step "🔧 启动 Worker"
    
    local conda_cmd=$(get_conda)
    
    export CY_LLM_ENGINE="$engine"
    [[ -n "$model" ]] && export CY_LLM_MODEL="$model"
    
    info "Engine: $engine"
    info "Port: $port"
    
    local serve_flag=""
    [[ "$serve" == true ]] && serve_flag="--serve"
    
    $conda_cmd run -n "$env_name" python "$WORKER_DIR/main.py" \
        --port "$port" \
        ${model:+--model "$model"} \
        $serve_flag
}

# =============================================================================
# 命令: lite - 启动轻量版服务 (Gateway Lite + Coordinator Lite + Worker)
# =============================================================================
cmd_lite() {
    local env_name="$DEFAULT_ENV_NAME"
    local port="$DEFAULT_LITE_PORT"
    local coordinator_port="$DEFAULT_LITE_COORDINATOR_PORT"
    local worker_port="$DEFAULT_LITE_WORKER_PORT"
    local engine="$DEFAULT_ENGINE"
    local model="${CY_LLM_MODEL:-}" # 从环境变量读取默认值
    local adapter=""
    local daemon=false

    while [[ $# -gt 0 ]]; do
        case $1 in
            --env)      env_name="$2"; shift 2 ;;
            --port)     port="$2"; shift 2 ;;
            --coordinator-port) coordinator_port="$2"; shift 2 ;;
            --worker-port) worker_port="$2"; shift 2 ;;
            --engine)   engine="$2"; shift 2 ;;
            --model)    model="$2"; shift 2 ;;
            --adapter|--checkpoint) adapter="$2"; shift 2 ;;
            -d|--daemon) daemon=true; shift ;;
            *) shift ;;
        esac
    done

    # 确定最终使用的模型 (入参 > 环境变量 > 默认模型)
    local target_model="${model:-$DEFAULT_MODEL}"

    step "⚡ 启动轻量版服务 (Gateway Lite + Coordinator Lite + Worker)"

    local conda_cmd=$(get_conda)
    local python_path=$(get_python_path "$env_name")
    if [[ -z "$python_path" ]]; then
        error "环境 '$env_name' 未找到，请先运行: ./cy-llm setup"
        exit 1
    fi

    info "Engine: $engine"
    info "Gateway Lite Port: $port"
    info "Coordinator Port: $coordinator_port"
    info "Worker Port: $worker_port"
    info "Python: $python_path"

    # 检查并尝试安装 Lite 依赖
    if ! $conda_cmd run -n "$env_name" python -c "import fastapi, uvicorn" &>/dev/null; then
        warn "检测到缺少 Lite 依赖，正在尝试安装..."
        $conda_cmd run -n "$env_name" pip install -r "$BACKEND_DIR/gateway_lite/requirements.txt" -r "$BACKEND_DIR/coordinator_lite/requirements.txt"
    fi

    export CY_LLM_ENGINE="$engine"
    export CY_LLM_DEFAULT_MODEL="$target_model"
    
    # 设置模型注册表路径，确保 Worker 能读到模型配置
    export CY_LLM_MODEL_REGISTRY_PATH="${CY_LLM_MODEL_REGISTRY_PATH:-$BACKEND_DIR/deploy/models.json}"
    
    if [[ -n "$adapter" ]]; then
        if [[ ! -d "$adapter" && -d "$TRAINING_DIR/checkpoints/$adapter" ]]; then
            adapter="$TRAINING_DIR/checkpoints/$adapter"
        fi
        
        # 如果是一个目录，尝试查找最新的 checkpoint-xxx 子目录
        if [[ -d "$adapter" ]]; then
            local latest=$(ls -d "$adapter"/checkpoint-* 2>/dev/null | sort -V | tail -n 1)
            if [[ -n "$latest" ]]; then
                adapter="$latest"
                info "自动定位最新训练节点: $adapter"
            else
                info "使用 Checkpoint 目录: $adapter"
            fi
        fi
        export CY_LLM_DEFAULT_ADAPTER="$adapter"
    fi
    
    export PYTHONPATH="$BACKEND_DIR"

    local coordinator_log="/tmp/cy-llm-lite-coordinator.log"
    local gateway_log="/tmp/cy-llm-lite-gateway.log"
    local worker_log="/tmp/cy-llm-lite-worker.log"

    local coord_env=(
        "PYTHONPATH=$BACKEND_DIR"
        "COORDINATOR_GRPC_BIND=0.0.0.0:${coordinator_port}"
        "WORKER_GRPC_ADDR=127.0.0.1:${worker_port}"
    )

    local gw_env=(
        "PYTHONPATH=$BACKEND_DIR"
        "COORDINATOR_GRPC_ADDR=127.0.0.1:${coordinator_port}"
        "GATEWAY_REQUEST_TIMEOUT=300"
    )

    # 准备 Worker 启动参数
    local worker_args=("--port" "$worker_port" "--serve" "--model" "$target_model")

    if [[ "$daemon" == true ]]; then
        info "后台模式启动..."
        (env "${coord_env[@]}" "$python_path" -u -m coordinator_lite.app.server > "$coordinator_log" 2>&1 & echo $! > /tmp/cy-llm-lite-coordinator.pid)
        (env "${gw_env[@]}" "$python_path" -m uvicorn gateway_lite.app.main:app --host 0.0.0.0 --port "$port" > "$gateway_log" 2>&1 & echo $! > /tmp/cy-llm-lite-gateway.pid)
        ("$python_path" -u -m worker.main "${worker_args[@]}" > "$worker_log" 2>&1 & echo $! > /tmp/cy-llm-lite-worker.pid)

        success "Coordinator PID: $(cat /tmp/cy-llm-lite-coordinator.pid)"
        success "Gateway PID: $(cat /tmp/cy-llm-lite-gateway.pid)"
        success "Worker PID: $(cat /tmp/cy-llm-lite-worker.pid)"
        return
    fi

    info "前台模式启动 (Ctrl+C 退出)"
    info "监控日志可通过 tail -f $gateway_log 查看"
    
    # 显式启动背景进程并捕获错误
    info "正在启动组件..."
    export CY_LLM_HEALTH_PORT=$((worker_port + 1000)) # 避免 9090 冲突

    env "${coord_env[@]}" "$python_path" -u -m coordinator_lite.app.server > "$coordinator_log" 2>&1 &
    local coord_pid=$!
    
    "$python_path" -u -m worker.main "${worker_args[@]}" > "$worker_log" 2>&1 &
    local worker_pid=$!

    trap 'kill $coord_pid $worker_pid 2>/dev/null || true; exit' INT TERM EXIT

    # 等待 Worker 启动并输出就绪状态
    info "正在等待本地推理引擎准备就绪..."
    local retry=0
    local max_retries=600  # 增加到 10 分钟，给模型下载留出时间
    local ready=false
    while [[ $retry -lt $max_retries ]]; do
        if grep -q "Worker gRPC 服务已启动" "$worker_log" 2>/dev/null; then
            ready=true
            break
        fi

        # 核心改进：实时检测显存不足关键字并中止
        if grep -qiE "显存不足|OutOfMemory|OOM|Insufficient VRAM" "$worker_log" 2>/dev/null; then
            echo ""
            error "检测到显存不足 (VRAM Insufficient)！"
            warn "最近的错误详细信息:"
            grep -iE "显存不足|OutOfMemory|OOM|Insufficient VRAM" "$worker_log" | tail -n 5 | sed 's/^/  /'
            info "正在清理残留进程..."
            cmd_stop
            exit 1
        fi
        
        # 检查进程是否还在运行
        if ! kill -0 $worker_pid 2>/dev/null; then
            echo ""
            error "Worker 进程已意外退出！"
            warn "最近的错误日志 ($worker_log):"
            tail -n 20 "$worker_log" | sed 's/^/  /'
            info "正在清理残留进程..."
            cmd_stop
            exit 1
        fi
        if ! kill -0 $coord_pid 2>/dev/null; then
            echo ""
            error "Coordinator 进程已意外退出！"
            warn "最近的错误日志 ($coordinator_log):"
            tail -n 20 "$coordinator_log" | sed 's/^/  /'
            info "正在清理残留进程..."
            cmd_stop
            exit 1
        fi

        sleep 1
        retry=$((retry + 1))
        echo -n "."
    done
    echo ""
    
    if [[ "$ready" != true ]]; then
        error "Worker 启动超时 ($max_retries 秒)。"
        warn "请检查日志: $worker_log"
        
        # 尝试输出最后几行日志以供参考
        if [[ -s "$worker_log" ]]; then
            warn "Worker 最后日志内容:"
            tail -n 15 "$worker_log" | sed 's/^/  /'
        else
            warn "Worker 日志文件为空，进程可能尚未输出任何内容或环境启动过慢。"
        fi

        warn "提示: 如果是第一次启动，驱动或模型 (${model:-$DEFAULT_MODEL}) 可能正在初始化/下载，请手动检查网络或显存。"
        
        # 自动执行停止操作
        info "超时，正在自动清理残留进程..."
        cmd_stop
        exit 1
    fi
    
    success "🚀 所有组件已就绪！"
    success "  - Gateway: http://0.0.0.0:$port (OpenAI API)"
    success "  - Coordinator: $coordinator_port (gRPC)"
    success "  - Worker: $worker_port (gRPC)"
    echo -e "你可以现在开始发送请求。按 ${CYAN}Ctrl+C${NC} 停止所有服务。\n"

    # 前台启动网关，方便直接看报错
    env "${gw_env[@]}" "$python_path" -m uvicorn gateway_lite.app.main:app --host 0.0.0.0 --port "$port"
}

# =============================================================================
# 命令: docker - Docker Compose 部署
# =============================================================================
cmd_docker() {
    local action="${1:-up}"
    shift || true
    local engine="$DEFAULT_ENGINE"
    local scale=1
    
    while [[ $# -gt 0 ]]; do
        case $1 in
            --engine)   engine="$2"; shift 2 ;;
            --scale)    scale="$2"; shift 2 ;;
            *) shift ;;
        esac
    done

    step "🐳 Docker 部署"
    
    cd "$DEPLOY_DIR"
    
    export CY_LLM_ENGINE="$engine"
        export CY_LLM_ENGINE="$engine"
    
    case "$action" in
        up)
            info "启动容器 (Engine: $engine, Scale: $scale)..."
            case "$engine" in
                ascend-*) docker compose --profile ascend up -d --scale worker-ascend="$scale" ;;
                *)        docker compose up -d --scale worker-nvidia="$scale" ;;
            esac
            success "容器启动完成"
            docker compose ps
            ;;
        down)
            info "停止容器..."
            docker compose --profile ascend down
            success "容器已停止"
            ;;
        logs)
            docker compose logs -f
            ;;
        ps)
            docker compose ps
            ;;
        build)
            info "构建镜像..."
            docker compose build
            success "镜像构建完成"
            ;;
        *)
            error "未知操作: $action"
            echo "可用操作: up, down, logs, ps, build"
            exit 1
            ;;
    esac
}

# =============================================================================
# 命令: stop - 停止服务
# =============================================================================
cmd_stop() {
    step "🛑 停止服务"
    
    if [[ -f /tmp/cy-llm-gateway.pid ]]; then
        local pid=$(cat /tmp/cy-llm-gateway.pid)
        if kill -0 "$pid" 2>/dev/null; then
            kill "$pid"
            rm /tmp/cy-llm-gateway.pid
            success "Gateway (PID $pid) 已停止"
        fi
    fi
    
    # 查找并停止所有相关进程
    pkill -f "gateway-.*-SNAPSHOT.jar" 2>/dev/null && info "Legacy Gateway 进程已停止" || true
    pkill -f "worker/main.py" 2>/dev/null && info "Worker 进程已停止" || true
    pkill -f "coordinator_lite.app.server" 2>/dev/null && info "Coordinator Lite 进程已停止" || true
    pkill -f "gateway_lite.app.main" 2>/dev/null && info "Gateway Lite 进程已停止" || true
    pkill -f "uvicorn.*gateway_lite" 2>/dev/null && info "Uvicorn Gateway 进程已停止" || true

    # 清理 PID 文件
    rm -f /tmp/cy-llm-lite-*.pid
    
    success "服务已停止"
}

# =============================================================================
# 命令: status - 查看状态
# =============================================================================
cmd_status() {
    step "📊 服务状态"
    
    echo -e "\n${CYAN}进程状态:${NC}"
    if pgrep -f "gateway-.*-SNAPSHOT.jar" >/dev/null; then
        success "Gateway: 运行中"
    else
        warn "Gateway: 未运行"
    fi
    
    if pgrep -f "worker/main.py" >/dev/null; then
        success "Worker: 运行中"
    else
        warn "Worker: 未运行"
    fi
    
    echo -e "\n${CYAN}端口监听:${NC}"
    ss -tlnp 2>/dev/null | grep -E ":8080|:50051" || echo "  无相关端口监听"
    
    echo -e "\n${CYAN}环境变量:${NC}"
    echo "  CY_LLM_ENGINE=${CY_LLM_ENGINE:-未设置}"
    echo "  CY_LLM_CONDA_ENV=${CY_LLM_CONDA_ENV:-$DEFAULT_ENV_NAME}"
}

cmd_diagnose() {
    local model_name="${CY_LLM_MODEL:-deepseek-v3}"
    local verbose=false

    # Parse arguments
    while [[ $# -gt 0 ]]; do
        case "$1" in
            --model)
                model_name="$2"
                shift 2
                ;;
            --verbose|-v)
                verbose=true
                shift
                ;;
            *)
                model_name="$1"
                shift
                ;;
        esac
    done

    step "🩺 CY-LLM 环境诊断: $model_name"

    export CY_LLM_MODEL="$model_name"
    export CY_LLM_MODEL_REGISTRY_PATH="$BACKEND_DIR/deploy/config.json"
    export CY_LLM_VERBOSE="$verbose"

    PYTHONPATH="$BACKEND_DIR" CY_LLM_MODEL="$model_name" CY_LLM_VERBOSE="$verbose" \
        python - <<'PY'
import os
from worker.config.config_loader import load_worker_config
from worker.utils.diagnostic import (
    check_vram_for_model,
    format_vram_report,
    gather_gpu_summary,
    check_dependencies,
    format_dependency_summary,
    check_version_compatibility,
    format_compatibility_warnings,
)
from worker.utils.vram_optimizer import suggest_batch_optimization, suggest_kv_cache_strategy, get_vram_stats

verbose = os.environ.get("CY_LLM_VERBOSE", "false").lower() == "true"

config = load_worker_config(registry_path=os.environ.get("CY_LLM_MODEL_REGISTRY_PATH"))
model_id = os.environ["CY_LLM_MODEL"]
spec = config.model_registry.get(model_id)
if spec is None:
    raise SystemExit(f"模型 {model_id} 未在注册表中定义")

gpu = gather_gpu_summary()
print("CY-LLM 环境诊断报告")
print("=====================")
print("硬件信息:")
if gpu.get("available"):
    print(f"  GPU: {gpu.get('name')} ({gpu.get('total')} 总, {gpu.get('free')} 可用)")
    print(f"  CUDA: {gpu.get('cuda')}")
else:
    print("  GPU: 未检测到可用 CUDA 设备")

# 对模型路径进行脱敏处理（非 verbose 模式）
def sanitize_path(path: str) -> str:
    """隐藏完整路径，仅显示模型名称"""
    if not verbose and path:
        parts = path.split("/")
        if len(parts) > 2:
            return f".../{parts[-2]}/{parts[-1]}" if len(parts[-1]) else f".../{parts[-2]}"
        return parts[-1] if parts else path
    return path

# 显示模型路径（脱敏）
model_path_display = sanitize_path(spec.model_path)
if not verbose:
    print("  (使用 --verbose 查看完整路径)")

report = check_vram_for_model(model_id, spec)
print("")
print(f"模型检测: {model_id}")
print(format_vram_report(report))

print("\n依赖检查:")
statuses = check_dependencies()
for line in format_dependency_summary(statuses):
    print(line)

print("\n版本兼容性检查:")
compat_warnings = check_version_compatibility()
for line in format_compatibility_warnings(compat_warnings):
    print(line)

# KV Cache 策略建议
print("\nKV Cache 预分配策略:")
free_gb, total_gb = get_vram_stats()
engine_value = getattr(spec.engine, "value", spec.engine)
engine_type = (engine_value or "cuda-vllm").lower()
kv_suggestions = suggest_kv_cache_strategy(
    kv_cache_gb=report.kv_cache_gb,
    available_vram_gb=free_gb,
    max_model_len=spec.max_model_len or 8192,
    current_gpu_util=spec.gpu_memory_utilization or 0.75,
    expected_qps=None,  # 可以从环境变量读取
    engine_type=engine_type
)
if kv_suggestions:
    for suggestion in kv_suggestions:
        print(f"  {suggestion}")
else:
    print("  （当前配置合理）")

# 批处理优化建议
print("\n批处理优化建议:")
batch_suggestions = suggest_batch_optimization(
    engine_type=engine_type,
    kv_cache_gb=report.kv_cache_gb,
    available_vram_gb=free_gb,
    max_model_len=spec.max_model_len or 8192
)
if batch_suggestions:
    for suggestion in batch_suggestions:
        print(f"  {suggestion}")
else:
    print("  （当前引擎无特定批处理优化建议）")
PY
}

# =============================================================================
# 命令: test - 运行测试
# =============================================================================
cmd_test() {
    local env_name="$DEFAULT_ENV_NAME"
    local type="${1:-all}"
    shift || true
    local sub_test="$1"
    
    step "🧪 运行测试"
    
    local conda_cmd=$(get_conda)
    
    case "$type" in
        integration|int)
            if [[ -n "$sub_test" ]]; then
                info "运行指定集成测试: $sub_test..."
                $conda_cmd run -n "$env_name" python -u "$BACKEND_DIR/tests/test_integration.py" --test "$sub_test"
            else
                info "运行集成测试 (默认: integration)..."
                # 如果没有子测试名，默认运行名为 integration 的核心测试，以避免交互失败
                $conda_cmd run -n "$env_name" python -u "$BACKEND_DIR/tests/test_integration.py" --test integration
            fi
            ;;
        unit)
            info "运行单元测试..."
            # 检查并安装测试依赖
            local missing_deps=()
            if ! $conda_cmd run -n "$env_name" python -c "import pytest" 2>/dev/null; then
                missing_deps+=("pytest")
            fi
            if ! $conda_cmd run -n "$env_name" python -c "import pytest_asyncio" 2>/dev/null; then
                missing_deps+=("pytest-asyncio")
            fi
            
            if [ ${#missing_deps[@]} -gt 0 ]; then
                warn "缺少测试依赖: ${missing_deps[*]}，正在安装..."
                $conda_cmd run -n "$env_name" pip install "${missing_deps[@]}" -q
            fi
            
            # 运行 worker 单元测试
            if [[ -d "$WORKER_DIR/tests" ]]; then
                $conda_cmd run -n "$env_name" python -m pytest "$WORKER_DIR/tests" -v --tb=short
            else
                warn "测试目录不存在: $WORKER_DIR/tests"
                exit 1
            fi
            ;;
        all)
            cmd_test integration
            cmd_test unit
            ;;
        *)
            error "未知测试类型: $type"
            echo "可用类型: integration, unit, all"
            exit 1
            ;;
    esac
}

# =============================================================================
# 命令: models - 模型管理
# =============================================================================
cmd_models() {
    local env_name="$DEFAULT_ENV_NAME"
    local action="${1:-list}"

    local conda_cmd=$(get_conda)

    case "$action" in
        list|ls)
            step "📦 可用模型"
            $conda_cmd run -n "$env_name" python "$WORKER_DIR/main.py" --list-models
            ;;
        *)
            error "未知操作: $action"
            echo "可用操作: list"
            exit 1
            ;;
    esac
}

# =============================================================================
# 命令: prepare - 数据预处理
# =============================================================================
cmd_prepare() {
    local env_name="$DEFAULT_ENV_NAME"
    local raw_dir=""
    local output_file=""
    local character="$DEFAULT_CHARACTER"
    
    while [[ $# -gt 0 ]]; do
        case $1 in
            --env)      env_name="$2"; shift 2 ;;
            --raw)      raw_dir="$2"; shift 2 ;;
            --out)      output_file="$2"; shift 2 ;;
            --char)     character="$2"; shift 2 ;;
            *) shift ;;
        esac
    done

    if [[ -z "$raw_dir" || -z "$output_file" ]]; then
        error "用法: ./cy-llm prepare --raw <raw_dir> --out <output_file> [--char <character>]"
        exit 1
    fi

    step "数据预处理: $character"
    
    local conda_cmd=$(get_conda)
    $conda_cmd run -n "$env_name" python "$TRAINING_DIR/src/dataset_converter.py" \
        --raw_dir "$raw_dir" \
        --output_file "$output_file" \
        --character "$character"
        
    success "预处理完成: $output_file"
}

# =============================================================================
# 命令: train - LoRA 微调训练
# =============================================================================
cmd_train() {
    local env_name="$DEFAULT_ENV_NAME"
    local model="$DEFAULT_MODEL"
    local dataset=""
    local settings=""
    local output=""
    local batch="$DEFAULT_BATCH_SIZE"
    local grad_accum="$DEFAULT_GRAD_ACCUM"
    local epochs="$DEFAULT_EPOCHS"
    
    while [[ $# -gt 0 ]]; do
        case $1 in
            --env)          env_name="$2"; shift 2 ;;
            --model)        model="$2"; shift 2 ;;
            --dataset)      dataset="$2"; shift 2 ;;
            --settings)     settings="$2"; shift 2 ;;
            --output)       output="$2"; shift 2 ;;
            --batch)        batch="$2"; shift 2 ;;
            --grad-accum)   grad_accum="$2"; shift 2 ;;
            --epochs)       epochs="$2"; shift 2 ;;
            *) shift ;;
        esac
    done

    if [[ -z "$dataset" || -z "$output" ]]; then
        error "用法: ./cy-llm train --dataset <path> --output <dir> [--model <id>] [--settings <path>] ..."
        exit 1
    fi

    step "开始 LoRA 微调训练"
    info "基座模型: $model"
    info "训练数据: $dataset"
    [[ -n "$settings" ]] && info "设定数据: $settings"
    info "输出目录: $output"

    local conda_cmd=$(get_conda)
    local -a cmd_args=(
        "$TRAINING_DIR/src/train_lora.py"
        --model_name "$model"
        --dataset_path "$dataset"
        --output_dir "$output"
        --batch_size "$batch"
        --grad_accum "$grad_accum"
        --epochs "$epochs"
    )
    
    [[ -n "$settings" ]] && cmd_args+=(--settings_path "$settings")

    $conda_cmd run -n "$env_name" python "${cmd_args[@]}"
    
    success "训练完成，权重保存至: $output"
}

# =============================================================================
# 命令: chat - 交互式 LoRA 推理
# =============================================================================
cmd_chat() {
    local env_name="$DEFAULT_ENV_NAME"
    local model="$DEFAULT_MODEL"
    local lora=""
    local character="$DEFAULT_CHARACTER"
    
    while [[ $# -gt 0 ]]; do
        case $1 in
            --env)      env_name="$2"; shift 2 ;;
            --model)    model="$2"; shift 2 ;;
            --lora)     lora="$2"; shift 2 ;;
            --char)     character="$2"; shift 2 ;;
            *) shift ;;
        esac
    done

    if [[ -z "$model" || -z "$lora" ]]; then
        error "用法: ./cy-llm chat --model <base_model> --lora <lora_path> [--char <character>]"
        exit 1
    fi

    step "进入交互式对话 ($character)"
    
    local conda_cmd=$(get_conda)
    $conda_cmd run -n "$env_name" python "$TRAINING_DIR/src/inference.py" \
        --base_model "$model" \
        --lora_path "$lora" \
        --character "$character"
}

# =============================================================================
# 命令: config - 配置管理
# =============================================================================
cmd_config() {
    local action="${1:-validate}"
    local config_file="${2:-$BACKEND_DIR/deploy/config.json}"

    case "$action" in
        validate)
            step "🔍 验证配置文件: $config_file"

            if [[ ! -f "$config_file" ]]; then
                error "配置文件不存在: $config_file"
                exit 1
            fi

            PYTHONPATH="$BACKEND_DIR" python - "$config_file" <<'PY'
import json
import sys
from pathlib import Path

config_path = Path(sys.argv[1])

# 检查 JSON 语法
try:
    with open(config_path) as f:
        config = json.load(f)
    print("✅ JSON 语法正确")
except json.JSONDecodeError as e:
    print(f"❌ JSON 语法错误: {e}")
    sys.exit(1)

# 检查必填字段
if "models" not in config:
    print("❌ 缺少 'models' 字段")
    sys.exit(1)

print(f"✅ 找到 {len(config['models'])} 个模型配置")

# 验证每个模型配置
errors = []
warnings = []
valid_engines = ["cuda-vllm", "cuda-vllm-async", "cuda-trt", "nvidia", "ascend-vllm", "ascend-mindie", "ascend"]
valid_quantization = ["awq", "gptq", "bitsandbytes", "fp8", "fp8_e5m2", None]

for model_name, model_config in config["models"].items():
    # 检查必填字段
    if "engine" not in model_config:
        errors.append(f"模型 '{model_name}': 缺少 'engine' 字段")
        continue
    if "model_path" not in model_config:
        errors.append(f"模型 '{model_name}': 缺少 'model_path' 字段")
        continue

    # 验证引擎类型
    engine = model_config["engine"]
    if engine not in valid_engines:
        warnings.append(f"模型 '{model_name}': 未知引擎类型 '{engine}'（支持: {', '.join(valid_engines)}）")

    # 验证量化方法
    quantization = model_config.get("quantization")
    if quantization and quantization not in valid_quantization:
        warnings.append(f"模型 '{model_name}': 未知量化方法 '{quantization}'（支持: awq, gptq, bitsandbytes, fp8）")

    # vLLM + bitsandbytes 冲突检查
    if "vllm" in engine and quantization == "bitsandbytes":
        errors.append(f"模型 '{model_name}': vLLM 引擎不支持 bitsandbytes 量化，请使用 AWQ/GPTQ 或切换到 nvidia 引擎")

    # 检查 gpu_memory_utilization
    gpu_util = model_config.get("gpu_memory_utilization")
    if gpu_util and gpu_util > 0.90:
        warnings.append(f"模型 '{model_name}': gpu_memory_utilization={gpu_util:.2f} 过高，可能导致 OOM（推荐 0.70-0.85）")

    print(f"  ✓ {model_name}: {engine} ({model_config['model_path']})")

# 输出结果
print()
if errors:
    print("❌ 发现配置错误:")
    for err in errors:
        print(f"  • {err}")
    sys.exit(1)

if warnings:
    print("⚠️  配置警告:")
    for warn in warnings:
        print(f"  • {warn}")

if not errors and not warnings:
    print("✅ 配置文件验证通过，无问题")
else:
    print("\n💡 建议修复警告以获得最佳性能")
PY
            ;;
        *)
            error "未知操作: $action"
            echo "可用操作: validate"
            exit 1
            ;;
    esac
}

# =============================================================================
# 命令: doctor - 环境诊断
# =============================================================================
cmd_doctor() {
    local env_name="${CY_LLM_CONDA_ENV:-$DEFAULT_ENV_NAME}"

    step "🔍 环境诊断"

    # 检查 Conda 环境
    echo -e "\n${CYAN}Conda 环境:${NC}"
    local conda_cmd=$(get_conda)
    if $conda_cmd env list | grep -q "^$env_name "; then
        echo "  ✅ 环境存在: $env_name"
    else
        echo "  ❌ 环境不存在: $env_name"
        echo "  运行: ./cy-llm setup --engine <engine_type>"
        return 1
    fi

    # 检查关键依赖
    echo -e "\n${CYAN}依赖检查:${NC}"
    $conda_cmd run -n "$env_name" python - <<'PY'
import sys
def check_import(module, name=None):
    try:
        mod = __import__(module)
        version = getattr(mod, "__version__", "未知")
        print(f"  ✅ {name or module}: {version}")
        return True
    except ImportError:
        print(f"  ❌ {name or module}: 未安装")
        return False

check_import("torch", "PyTorch")
check_import("transformers")
check_import("vllm", "vLLM")
try:
    __import__("tensorrt_llm")
    print("  ✅ TensorRT-LLM: 已安装")
except ImportError:
    print("  ❌ TensorRT-LLM: 未安装")
PY

    # 检查 GPU
    echo -e "\n${CYAN}GPU 状态:${NC}"
    if command -v nvidia-smi &> /dev/null; then
        nvidia-smi --query-gpu=name,memory.total,memory.free --format=csv,noheader 2>/dev/null | \
            awk -F', ' '{printf "  GPU: %s (%s 总, %s 可用)\n", $1, $2, $3}' || \
            echo "  ⚠️  无法获取 GPU 信息"
    else
        echo "  ❌ nvidia-smi 不可用"
    fi

    echo ""
    success "诊断完成"
}

# =============================================================================
# 命令: convert-trt - 模型转换为 TensorRT 引擎
# =============================================================================
cmd_convert_trt() {
    local model=""
    local output=""
    local env_name="${CY_LLM_CONDA_ENV:-$DEFAULT_ENV_NAME}"

    while [[ $# -gt 0 ]]; do
        case "$1" in
            --model) model="$2"; shift 2 ;;
            --output) output="$2"; shift 2 ;;
            --env) env_name="$2"; shift 2 ;;
            *) shift ;;
        esac
    done

    if [[ -z "$model" || -z "$output" ]]; then
        error "用法: ./cy convert-trt --model <model> --output <output>"
        error "示例: ./cy convert-trt --model Qwen/Qwen2.5-7B-Instruct --output /models/qwen2.5-7b-trt"
        exit 1
    fi

    step "🔧 转换模型为 TensorRT-LLM 引擎"

    conda run -n "$env_name" python "$SCRIPT_DIR/scripts/convert_trt.py" \
        --model "$model" \
        --output "$output" \
        "$@"
}

# =============================================================================
# 命令: help - 帮助信息
# =============================================================================
cmd_help() {
    cat << 'EOF'
CY-LLM Engine - 统一部署命令行工具

用法: ./cy-llm <command> [options]

命令:
  setup       初始化环境 (Conda + 依赖 + Gateway 构建)
  start       启动完整服务 (Gateway + Worker)
    lite        启动轻量版服务 (Gateway Lite + Coordinator Lite + Worker)
  worker      仅启动 Worker
  stop        停止所有服务
  status      查看服务状态
  diagnose    环境与模型诊断 (VRAM、依赖、兼容性)
  doctor      环境诊断 (简化版)
  config      配置文件管理 (validate 验证配置)
  convert-trt 转换模型为 TensorRT-LLM 引擎
  docker      Docker Compose 部署
  test        运行测试
  models      模型管理
  prepare     数据预处理 (dataset_converter)
  train       LoRA 微调训练 (train_lora)
  chat        交互式 LoRA 推理测试 (inference)
  help        显示帮助信息

常用选项:
    --env NAME        Conda 环境名称 (默认: cy_llm_worker)
  --engine TYPE     推理引擎类型:
                      cuda-vllm      - NVIDIA GPU + vLLM (推荐)
                      cuda-trt       - NVIDIA GPU + TensorRT-LLM
                      nvidia         - NVIDIA GPU + Transformers + bitsandbytes
                      ascend-vllm    - 华为 NPU + vLLM-Ascend
                      ascend-mindie  - 华为 NPU + MindIE Turbo
  --port PORT       Gateway 端口 (默认: 8080)
  --skip-deps       跳过依赖安装
  --skip-gradle     跳过 Gateway 构建

示例:
  # 初始化 vLLM 环境
  ./cy-llm setup --engine cuda-vllm

  # 初始化 TRT 环境
  ./cy-llm setup --engine cuda-trt

  # 诊断环境与模型
  ./cy-llm diagnose deepseek-v3

  # 验证配置文件
  ./cy-llm config validate

  # 转换模型为 TRT 引擎
  ./cy-llm convert-trt --model Qwen/Qwen2.5-7B-Instruct --output /models/qwen2.5-7b-trt

  # 启动服务
  ./cy-llm start --engine cuda-vllm

    # 启动轻量版服务 (开源版 Lite)
    ./cy-llm lite --engine cuda-vllm --model deepseek-v3

  # 后台启动
  ./cy-llm start -d

  # Docker 部署
  ./cy-llm docker up --engine cuda-vllm --scale 2

  # 查看状态
  ./cy-llm status

  # 数据预处理
  ./cy-llm prepare --raw ./raw_data --out ./data/train.jsonl --char 芙宁娜

  # 启动 LoRA 训练
  ./cy-llm train --dataset ./data/train.jsonl --output ./checkpoints/lora_v1 --model facebook/opt-2.7b

  # 交互式对话测试
  ./cy-llm chat --model facebook/opt-2.7b --lora ./checkpoints/lora_v1

环境变量:
    CY_LLM_ENGINE          默认推理引擎
    CY_LLM_CONDA_ENV       默认 Conda 环境名
    CY_LLM_PORT            默认 Gateway 端口
    CY_LLM_WORKER_PORT     默认 Worker 端口
    CY_LLM_LITE_PORT        Lite Gateway 端口
    CY_LLM_LITE_COORDINATOR_PORT  Lite Coordinator 端口
    CY_LLM_LITE_WORKER_PORT       Lite Worker 端口

更多信息: https://github.com/your-repo/CY-LLM-Engine
EOF
}

# =============================================================================
# 主入口
# =============================================================================
main() {
    local command="${1:-help}"
    shift || true
    
    case "$command" in
        setup)      cmd_setup "$@" ;;
        start)      cmd_start "$@" ;;
        lite)       cmd_lite "$@" ;;
        worker)     cmd_worker "$@" ;;
        docker)     cmd_docker "$@" ;;
        stop)       cmd_stop "$@" ;;
        status)     cmd_status "$@" ;;
        diagnose)   cmd_diagnose "$@" ;;
        doctor)     cmd_doctor "$@" ;;
        config)     cmd_config "$@" ;;
        convert-trt) cmd_convert_trt "$@" ;;
        test)       cmd_test "$@" ;;
        models)     cmd_models "$@" ;;
        prepare)    cmd_prepare "$@" ;;
        train)      cmd_train "$@" ;;
        chat)       cmd_chat "$@" ;;
        help|--help|-h) cmd_help ;;
        *)
            error "未知命令: $command"
            echo "运行 './cy-llm help' 查看帮助"
            exit 1
            ;;
    esac
}

main "$@"
